source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '12.0'

target 'VPSNMobile' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VPSNMobile
	pod 'TensorFlowLiteSwift', '2.6.0', :subspecs => ['Metal']
end
